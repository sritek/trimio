{
  "build": {
    "builder": "railpack",
    "buildCommand": "pnpm install --frozen-lockfile && cd apps/api && pnpm prisma generate && cd ../.. && pnpm turbo run build --filter=@trimio/shared && pnpm turbo run build --filter=@trimio/api"
  },
  "deploy": {
    "startCommand": "cd apps/api && node dist/server.js",
    "restartPolicyMaxRetries": 5,
    "restartPolicyWindowMs": 60000
  }
}
